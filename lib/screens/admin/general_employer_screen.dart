import 'package:flutter/material.dart';

class GeneralEmployerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Employer management features coming soon.',
        style: TextStyle(fontSize: 16),
      ),
    );
  }
}
